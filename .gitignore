# Graphify output — a local cache rebuilt by the post-commit hook on every commit.
# Not tracked (it would be perpetually dirty); regenerated automatically.
graphify-out/

# Windows/WSL alternate-data-stream artifacts
*:Zone.Identifier
